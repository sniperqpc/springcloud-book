spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
  profiles:
    active: pro

logging:
  file: build/${spring.application.name}.log